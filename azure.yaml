# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: AspireToAutomate
services:  
  app:
    language: dotnet
    project: .\AspireToAutomate.AppHost\AspireToAutomate.AppHost.csproj
    host: containerapp
